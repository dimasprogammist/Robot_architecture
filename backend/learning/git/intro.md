---
id: git-intro
title: Git как память системы
category: git
section: Основы
order: 1
description: Репозиторий кода, конфигов, не бинарных STL гигабайтов без LFS.
tags: [git, основы]
technologies: [Git]
related: [git-commit, git-tag, git-lfs]
---

# Git как память системы

Git фиксирует, почему изменился протокол. Canvas-экспорт JSON тоже можно версионировать.

## Зачем это в робототехнической системе

Прошивка, шлюз, описания регистров — один монорепо или несколько с версиями на связях.

## Синтаксис и контракт

```bash
git status
git log --oneline
```

## Типичные ошибки

- коммиты «fix» без смысла
- секреты в репозитории

## В Architecture Canvas

current_version_label проекта Canvas держите рядом с git tag.

## Связанные разделы
- git-commit
- git-tag
- git-lfs
